Check who is logged in
Code:
last |grep "logged in"
Check <PID> for this user
Code:
ps -aux | grep ssh | grep pts/0
Kill SSH user

kill -9 <PID>

SSH without Key Authentication:

ssh -o StrictHostKeyChecking=no [DOMAIN_OR_IP]
#alias chunari=date
#alias r='cd /'

To check date just simply type chunari

#chunari

Remove Alias:

#unalias chunari

It will be reset after reboot so you need to edit bashrc file for permanent entry.


nano ~/.bachrc

alias minikube-start="minikube start --vm-driver kvm"
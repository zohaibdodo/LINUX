TO BREAK ROOT PASSWORD:

Interrupt boot process and go to end of kernal line and remove rhgb and add "rd.break enforcing=0"

mount -o remount,rw /sysroot
chroot /sysroot
password root

Question 2: (Reposatory)

To check reposatory goto cd /etc/yum.repos.d/
dnf config-manager --add-repo http://repo.eight.... (URL)

dnf repolist

Question 3:
timedatectl set-timezone "Asia/Karachi"
timedatectl set-ntp yes
rpm -qa | grep chrony
dnf install -y chrony

systemctl status chronyd

Question 4: (Network)

ifconfig
cd /etc/sysconfig/network-scripts/
cat ifcfg-eth1

nmcli connection show
nmcli connection modify System\ eth1 +ipv4.address 10.0.0.6/24
nmcli connection reload

nmcli modify System\ eth1 ipv6.method manual ipv6.address fd01::100/64
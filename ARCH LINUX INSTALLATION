lsblk

cfdisk /dev/sda

gpt

Create a 1M and set type boot partition
Create a 4G and set swap partition
Create a 16G and set Linux partition

mkfs.ext4 /dev/sda3
mkfs.swap /dev/sda2

swapon -a

mount /dev/sda3 /mnt

pacstrap /mnt base linux linux-firmware nano grub dhcpcd


genfstab /mnt

genfstab /mnt >> /mnt/etc/fstab

arch chroot /mnt
passwd root

grub-mkconfig -o /boot/grub/grub.cfg
grub-install /dev/sda
